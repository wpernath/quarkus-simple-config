namePrefix: dev-
commonLabels:
  variant: development



# apply a patch to replace the Host of the Route
#- patch: |-
#    - op: replace
#      path: /spec/host
#      value: art-test.apps-crc.testing
#  target:
#    kind: Route
# this patch needs to be done, because kustomize does not change 
# the route target service name
patches:
- patch: |-
    - op: replace
      path: /spec/to/name
      value: dev-simple
  target:
    kind: Route
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- ../../base
images:
- digest: sha256:749e79aafeb91e99bbdfd7b4e6f2a480490c1e2885e53388fc69b5759b8c9d42
  name: quay.io/wpernath/simple-quarkus:latest
  newName: quay.io/wpernath/quarkus-simple-wow
